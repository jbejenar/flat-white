/**
 * flat-white — Australian addresses. Flattened and served.
 *
 * This is the main entry point. The build pipeline orchestrates:
 *   download → load → flatten → split → compress → output
 *
 * See ROADMAP.md for the full architecture and ticket breakdown.
 */

export const VERSION = "0.1.0";
